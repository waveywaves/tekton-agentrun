---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentconfigs.agent.tekton.dev
spec:
  group: agent.tekton.dev
  names:
    kind: AgentConfig
    listKind: AgentConfigList
    plural: agentconfigs
    singular: agentconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.maxIterations
      name: Max Iterations
      type: integer
    - jsonPath: .spec.serviceAccount
      name: Service Account
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentConfig defines the configuration for agent execution
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentConfigSpec defines the desired state of AgentConfig
            properties:
              configPVC:
                description: ConfigPVC is the name of the PVC containing prompts,
                  schemas, and policies
                minLength: 1
                type: string
              maxIterations:
                description: MaxIterations is the maximum number of plan-act-reflect
                  iterations
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              networkPolicy:
                description: NetworkPolicy defines the network isolation mode
                enum:
                - strict
                - permissive
                type: string
              policy:
                description: Policy defines the OPA policy enforcement mode
                properties:
                  opa:
                    description: OPA defines the policy enforcement mode
                    enum:
                    - strict
                    - permissive
                    type: string
                type: object
              postHooks:
                description: PostHooks are the names of hook pods to run after agent
                  execution
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              preHooks:
                description: PreHooks are the names of hook pods to run before agent
                  execution
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              provider:
                description: Provider specifies which LLM provider to use
                enum:
                - claude
                - gemini
                type: string
              serviceAccount:
                description: ServiceAccount to use for agent pod execution
                type: string
              timeout:
                description: Timeout is the maximum duration for agent execution
                type: string
            required:
            - configPVC
            type: object
          status:
            description: AgentConfigStatus defines the observed state of AgentConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the AgentConfig's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
